"""A result model used to return credentials of a user from server."""
type Credentials {
    """The login of the user."""
    login: String!,
    """The encrypted password of the user."""
    password: String!
}
