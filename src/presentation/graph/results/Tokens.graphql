"""A result model used to return the JWT's after a success authentication."""
type Tokens {
    "A JWT used for authentication and authorization."
    accessToken: String!

    "A JWT used for refresh an expired access token."
    refreshToken: String!

    "The expiration time of the token in seconds."
    expireIn: Int!
}
